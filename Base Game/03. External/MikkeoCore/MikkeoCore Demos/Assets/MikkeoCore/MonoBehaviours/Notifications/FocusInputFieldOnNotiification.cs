using UnityEngine;
using System.Collections;

public class FocusInputFieldOnNotification : MonoBehaviour
{

	/*
	private UIInput uiInput;
	public string identifier;
	
	// Use this for initialization
	void Start ()
	{
		NotificationCenter.DefaultCenter.AddObserver (this, "OnSetFocusToInputField");
		uiInput = gameObject.GetComponent<UIInput> ();
	}
	
	void OnSetFocusToInputField (Notification n)
	{
		
		string id = (string)n.data ["identifier"];
		bool isFocused = (bool)n.data ["isFocused"];
		
		if (id == identifier) {
			if (isFocused) {
				Debug.Log (identifier + "--> I'm focused!");			
				uiInput.isSelected = true;	
			} else {
				Debug.Log (identifier + "--> I'm UNfocused!");			
				uiInput.isSelected = false;	
			}
		} else {
			Debug.Log (identifier + "--> I'm not... |" + identifier + "|" + id);
		}
	}
	
	*/
	
}

using UnityEngine;
using System.Collections;

public class UpdateSliderOnNotification : MonoBehaviour
{
	/*
	private UISlider uiSlider;
	public string identifier;
	
	// Use this for initialization
	void Start ()
	{	
		NotificationCenter.DefaultCenter.AddObserver (this, "OnChangeSlider");	
		uiSlider = gameObject.GetComponent<UISlider> ();
	}
	
	void OnChangeText (Notification n)
	{
		Debug.Log ("Changing text to: " + (string)n.data ["text"]);
		string id = (string)n.data ["identifier"];
		if (id == identifier)
			uiSlider.value = (float)n.data ["sliderValue"];
	}
	*/
	
}
